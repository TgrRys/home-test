# Railway Deployment Configuration

# Railway will automatically set these environment variables when you add a PostgreSQL service
# You only need to manually set these variables:

# Server Configuration
PORT=3000
NODE_ENV=production

# JWT Configuration
JWT_SECRET=nutech-test-secret
JWT_EXPIRES_IN=12h

# Railway will automatically provide:
# DATABASE_URL (PostgreSQL connection string)
# PGHOST, PGPORT, PGUSER, PGPASSWORD, PGDATABASE (PostgreSQL credentials)

# Note: Railway's PostgreSQL service automatically provides the DATABASE_URL
# which includes all database connection details in a single connection string
